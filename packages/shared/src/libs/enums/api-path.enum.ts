const APIPath = {
	API_TOKENS: "/api-tokens",
	AUTH: "/auth",
	COMPOSED_PROMPTS: "/composed-prompts",
	HEALTH: "/health",
	PROMPTS: "/prompts",
	USERS: "/users",
	WORKSPACES: "/workspaces",
} as const;

export { APIPath };
