const EmbeddingErrorMessage = {
	MODEL_FAILED: "Embedding model failed to provision — do not wait for it.",
	MODEL_NOT_READY: "Embedding model is not ready yet — retry later.",
} as const;

export { EmbeddingErrorMessage };
