const PAGE_INCREMENT = 1;
const SEARCH_DELAY_MS = 300;
const FRACTION_DIGITS = 1;

export { DEFAULT_PROMPT_FILTERS } from "./default-prompt-filters.constant.js";
export { FRACTION_DIGITS, PAGE_INCREMENT, SEARCH_DELAY_MS };
