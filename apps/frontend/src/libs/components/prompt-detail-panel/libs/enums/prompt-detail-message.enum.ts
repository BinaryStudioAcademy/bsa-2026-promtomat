const PromptDetailMessage = {
	COPY_FAILURE:
		"Could not copy the prompt. Try again or select the text manually.",
	COPY_SUCCESS: "Prompt copied to clipboard.",
	UPDATE_INTENT_SUCCESS: "Task intent was updated.",
} as const;

export { PromptDetailMessage };
