import { type ComposeRequestDto } from "~/modules/composed-prompts/composed-prompts.js";

const DEFAULT_GENERATE_PAYLOAD: Partial<ComposeRequestDto> = {
	description: "",
};

export { DEFAULT_GENERATE_PAYLOAD };
